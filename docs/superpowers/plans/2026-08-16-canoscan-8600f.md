# CanoScan 8600F Driver Stack + scan8600 CLI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Selbst kompilierter SANE-Treiberstack (genesys) plus Python-CLI `scan8600` für Flachbett- und Durchlicht-Scans mit dem Canon CanoScan 8600F auf macOS arm64.

**Architecture:** sane-backends wird aus dem Quellcode gebaut und repo-lokal unter `./prefix` installiert (nur genesys-Backend). Die CLI ruft das lokale `scanimage` per subprocess, TIFF nativ, PNG/JPEG via Pillow.

**Tech Stack:** autotools, libusb (Homebrew), sane-backends 1.3.1, Python ≥3.13, Pillow, pytest.

**Spec:** `docs/superpowers/specs/2026-08-16-canoscan-8600f-design.md`

## Global Constraints

- Kein System-Install: alles unter `~/Documents/GitHub/canoscan` bzw. `./prefix`.
- Nur genesys-Backend (`BACKENDS=genesys`).
- Python nur stdlib + Pillow (Tests: pytest).
- Default-Format TIFF; Defaults 300 dpi (flatbed) / 1200 dpi (film).
- Hardware-Tests brauchen den angeschlossenen 8600F (USB 04a9:2229).

---

### Task 1: build_sane.sh — Treiber-Build

**Files:**
- Create: `build_sane.sh`

**Interfaces:**
- Produces: `prefix/bin/scanimage`, `prefix/etc/sane.d/`, `prefix/lib/sane/libsane-genesys.*` — spätere Tasks rufen `prefix/bin/scanimage` mit `SANE_CONFIG_DIR=prefix/etc/sane.d`.

- [ ] **Step 1: Skript schreiben**

```bash
#!/usr/bin/env bash
# Baut sane-backends (nur genesys) repo-lokal nach ./prefix. Idempotent.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
SRC="$ROOT/vendor/sane-backends"
PREFIX="$ROOT/prefix"
TAG="1.3.1"

brew list --versions libusb libtiff jpeg-turbo libpng >/dev/null || \
  brew install libusb libtiff jpeg-turbo libpng

if [ ! -d "$SRC" ]; then
  git clone --depth 1 --branch "$TAG" \
    https://gitlab.com/sane-project/backends.git "$SRC"
fi

cd "$SRC"
[ -x ./configure ] || ./autogen.sh
PKG_CONFIG_PATH="$(brew --prefix)/lib/pkgconfig" \
  ./configure --prefix="$PREFIX" BACKENDS=genesys \
    --disable-shared-first --without-snmp --without-avahi
make -j"$(sysctl -n hw.ncpu)"
make install
"$PREFIX/bin/scanimage" --version
```

- [ ] **Step 2: Ausführen**

Run: `chmod +x build_sane.sh && ./build_sane.sh`
Expected: endet mit `scanimage ... backend version 1.3.1`. Falls configure-Flags nicht existieren (`--disable-shared-first` etc.), Flag entfernen und erneut laufen lassen — Kernanforderung ist nur `--prefix` + `BACKENDS=genesys`.

- [ ] **Step 3: Verifizieren**

Run: `ls prefix/bin/scanimage prefix/lib/sane/ && SANE_CONFIG_DIR=prefix/etc/sane.d prefix/bin/scanimage -L`
Expected: `device 'genesys:libusb:...' is a Canon CanoScan 8600F flatbed scanner` (Scanner angeschlossen).

- [ ] **Step 4: Commit**

```bash
printf 'vendor/\nprefix/\n__pycache__/\n.venv/\n*.tiff\n*.png\n*.jpg\n' > .gitignore
git add .gitignore build_sane.sh
git commit -m "feat: repo-local sane-backends build (genesys only)"
```

---

### Task 2: scan8600 — Kommandoaufbau (TDD)

**Files:**
- Create: `scan8600.py`
- Test: `tests/test_scan8600.py`

**Interfaces:**
- Consumes: Pfadkonvention aus Task 1 (`prefix/bin/scanimage`).
- Produces: `build_command(args, source_name) -> list[str]`, `parse_args(argv) -> argparse.Namespace` (Attribute: mode, dpi, format, output, gray), Konstanten `PREFIX`, `SCANIMAGE`, `DEFAULT_DPI = {"flatbed": 300, "film": 1200}`.

- [ ] **Step 1: Failing Tests schreiben**

```python
# tests/test_scan8600.py
import scan8600


def test_default_dpi_flatbed():
    a = scan8600.parse_args(["--mode", "flatbed"])
    assert a.dpi == 300 and a.format == "tiff" and not a.gray


def test_default_dpi_film():
    a = scan8600.parse_args(["--mode", "film"])
    assert a.dpi == 1200


def test_build_command_flatbed_color():
    a = scan8600.parse_args(["--mode", "flatbed", "--dpi", "600"])
    cmd = scan8600.build_command(a, source_name=None)
    assert cmd[0] == str(scan8600.SCANIMAGE)
    assert "--format=tiff" in cmd and "--resolution" in cmd
    assert cmd[cmd.index("--resolution") + 1] == "600"
    assert cmd[cmd.index("--mode") + 1] == "Color"
    assert "--source" not in cmd


def test_build_command_film_gray_sets_source():
    a = scan8600.parse_args(["--mode", "film", "--gray"])
    cmd = scan8600.build_command(a, source_name="Transparency Adapter")
    assert cmd[cmd.index("--source") + 1] == "Transparency Adapter"
    assert cmd[cmd.index("--mode") + 1] == "Gray"


def test_default_output_name_has_extension():
    a = scan8600.parse_args(["--mode", "flatbed", "--format", "png"])
    assert scan8600.default_output(a).endswith(".png")
```

- [ ] **Step 2: Fail verifizieren**

Run: `python3 -m pytest tests/ -q` (aus Repo-Root; vorher `python3 -m venv .venv && .venv/bin/pip -q install pytest pillow` und `.venv/bin/python -m pytest` nutzen)
Expected: FAIL / ModuleNotFoundError.

- [ ] **Step 3: Implementieren**

```python
#!/usr/bin/env python3
"""scan8600 - Flachbett- und Durchlicht-Scans mit dem CanoScan 8600F."""
import argparse
import datetime
import pathlib
import re
import subprocess
import sys

PREFIX = pathlib.Path(__file__).resolve().parent / "prefix"
SCANIMAGE = PREFIX / "bin" / "scanimage"
SANE_ENV = {"SANE_CONFIG_DIR": str(PREFIX / "etc" / "sane.d")}
DEFAULT_DPI = {"flatbed": 300, "film": 1200}


def parse_args(argv):
    p = argparse.ArgumentParser(prog="scan8600")
    p.add_argument("--mode", required=True, choices=["flatbed", "film"])
    p.add_argument("--dpi", type=int)
    p.add_argument("--format", choices=["tiff", "png", "jpeg"], default="tiff")
    p.add_argument("--output")
    p.add_argument("--gray", action="store_true")
    a = p.parse_args(argv)
    if a.dpi is None:
        a.dpi = DEFAULT_DPI[a.mode]
    return a


def default_output(a):
    ext = {"tiff": "tiff", "png": "png", "jpeg": "jpg"}[a.format]
    stamp = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
    return f"scan_{stamp}.{ext}"


def build_command(a, source_name):
    cmd = [str(SCANIMAGE), "--format=tiff",
           "--resolution", str(a.dpi),
           "--mode", "Gray" if a.gray else "Color"]
    if source_name:
        cmd += ["--source", source_name]
    return cmd
```

- [ ] **Step 4: Pass verifizieren**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add scan8600.py tests/test_scan8600.py
git commit -m "feat: scan8600 argument parsing and command construction"
```

---

### Task 3: Source-Erkennung und Fehlerpfade (TDD)

**Files:**
- Modify: `scan8600.py`
- Test: `tests/test_discovery.py`

**Interfaces:**
- Consumes: `SCANIMAGE`, `SANE_ENV` aus Task 2.
- Produces: `find_film_source(options_text: str) -> str | None`, `run_scan(a) -> pathlib.Path` (wirft `ScanError(str)`), Klasse `ScanError(Exception)`.

- [ ] **Step 1: Failing Tests schreiben**

```python
# tests/test_discovery.py
import pytest
import scan8600

OPTIONS = """
    --source Flatbed|Transparency Adapter [Flatbed]
        Selects the scan source
"""


def test_find_film_source():
    assert scan8600.find_film_source(OPTIONS) == "Transparency Adapter"


def test_find_film_source_missing():
    assert scan8600.find_film_source("--source Flatbed [Flatbed]") is None


def test_run_scan_device_not_found(monkeypatch, tmp_path):
    def fake_run(cmd, **kw):
        class R:
            returncode = 1
            stderr = b"no SANE devices found"
            stdout = b""
        return R()
    monkeypatch.setattr(scan8600.subprocess, "run", fake_run)
    a = scan8600.parse_args(
        ["--mode", "flatbed", "--output", str(tmp_path / "x.tiff")])
    with pytest.raises(scan8600.ScanError, match="nicht gefunden"):
        scan8600.run_scan(a)
```

- [ ] **Step 2: Fail verifizieren**

Run: `.venv/bin/python -m pytest tests/test_discovery.py -q`
Expected: FAIL, `find_film_source` fehlt.

- [ ] **Step 3: Implementieren (an scan8600.py anhängen)**

```python
class ScanError(Exception):
    pass


def find_film_source(options_text):
    m = re.search(r"--source\s+(\S[^\[\n]*)", options_text)
    if not m:
        return None
    for cand in m.group(1).split("|"):
        cand = cand.strip()
        if re.search(r"transparen|film|ta\b", cand, re.IGNORECASE):
            return cand
    return None


def scanimage_run(cmd, **kw):
    import os
    env = dict(os.environ, **SANE_ENV)
    return subprocess.run(cmd, env=env, capture_output=True, **kw)


def run_scan(a):
    out = pathlib.Path(a.output or default_output(a))
    source = None
    if a.mode == "film":
        probe = scanimage_run([str(SCANIMAGE), "-A"])
        source = find_film_source(probe.stdout.decode(errors="replace"))
        if source is None:
            raise ScanError(
                "Keine Durchlicht-Quelle gefunden. Verfügbare Optionen:\n"
                + probe.stdout.decode(errors="replace"))
    r = scanimage_run(build_command(a, source))
    if r.returncode != 0:
        err = r.stderr.decode(errors="replace")
        if "no SANE devices" in err or "Invalid argument" in err:
            raise ScanError(
                "Scanner nicht gefunden. USB-Kabel prüfen, dann: "
                "ioreg -p IOUSB | grep -i CanoScan. Details:\n" + err)
        raise ScanError(err)
    tiff_bytes = r.stdout
    if a.format == "tiff":
        out.write_bytes(tiff_bytes)
    else:
        import io
        from PIL import Image
        img = Image.open(io.BytesIO(tiff_bytes))
        img.save(out, quality=95)
    return out


def main(argv=None):
    a = parse_args(argv if argv is not None else sys.argv[1:])
    try:
        out = run_scan(a)
    except ScanError as e:
        print(f"scan8600: {e}", file=sys.stderr)
        return 1
    print(out)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

Hinweis: `run_scan` ruft intern `scanimage_run`; der Test patcht `scan8600.subprocess.run`, was durch `capture_output=True` weiter funktioniert — fake_run akzeptiert `**kw`.

- [ ] **Step 4: Pass verifizieren**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: 8 passed.

- [ ] **Step 5: Commit**

```bash
git add scan8600.py tests/test_discovery.py
git commit -m "feat: film source discovery, scan execution, error paths"
```

---

### Task 4: Hardware-Verifikation + README

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: kompletter Stack aus Task 1-3.

- [ ] **Step 1: Erkennung**

Run: `SANE_CONFIG_DIR=prefix/etc/sane.d prefix/bin/scanimage -L`
Expected: `genesys:libusb:...  Canon CanoScan 8600F`.

- [ ] **Step 2: Flachbett-Testscan**

Run: `.venv/bin/python scan8600.py --mode flatbed --dpi 300 --output test_flatbed.tiff`
Expected: Exit 0, Datei > 1 MB. Erster Lauf kalibriert (dauert länger).

- [ ] **Step 3: Durchlicht-Testscan**

Run: `.venv/bin/python scan8600.py --mode film --dpi 1200 --output test_film.tiff`
Expected: Exit 0, TPU-Lampe leuchtet, Datei entsteht. Ohne eingelegten Film ist das Bild leer/orange — akzeptabel für den Smoke-Test.

- [ ] **Step 4: README schreiben**

```markdown
# canoscan

Eigener Treiber-Stack für den Canon CanoScan 8600F auf macOS arm64.

## Setup
    ./build_sane.sh                  # kompiliert sane-backends (genesys) nach ./prefix
    python3 -m venv .venv && .venv/bin/pip install pytest pillow

## Scannen
    .venv/bin/python scan8600.py --mode flatbed                # 300 dpi, TIFF
    .venv/bin/python scan8600.py --mode film --dpi 2400        # Durchlicht
    Optionen: --format tiff|png|jpeg, --output PATH, --gray

## Tests
    .venv/bin/python -m pytest tests/ -q
```

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: setup and usage"
```
